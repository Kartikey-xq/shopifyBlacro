<script
  src="{{ 'product-recommendations.js' | asset_url }}"
  type="module"
  fetchpriority="low"
></script>

{% liquid
  # Check if current product belongs to the 'relic' collection
  assign relic_collection = nil
  for coll in product.collections
    if coll.handle == 'relic'
      assign relic_collection = coll
      break
    endif
  endfor

  assign has_custom_products = false
  if relic_collection != blank or section.settings.collection != blank
    assign has_custom_products = true
  endif
-%}

{% liquid
  # Determine aspect ratio and column counts
  assign cols_desktop = section.settings.columns | plus: 0
  assign cols_tablet = 3
  if cols_desktop < 3
    assign cols_tablet = cols_desktop
  endif
  assign cols_mobile = section.settings.mobile_columns | plus: 0

  assign aspect_ratio = '3 / 4'
  case section.settings.image_ratio
    when 'square'
      assign aspect_ratio = '1 / 1'
    when 'portrait-alt'
      assign aspect_ratio = '4 / 5'
    when 'landscape'
      assign aspect_ratio = '16 / 9'
  endcase

  assign desktop_image_height = 'auto'
  if section.settings.desktop_card_height == 'small'
    assign desktop_image_height = '280px'
  elsif section.settings.desktop_card_height == 'medium'
    assign desktop_image_height = '380px'
  elsif section.settings.desktop_card_height == 'large'
    assign desktop_image_height = '480px'
  elsif section.settings.desktop_card_height == 'custom'
    assign desktop_image_height = section.settings.desktop_card_custom_height | append: 'px'
  endif
%}

{% style %}
  .section-product-recommendations-wrapper {
    padding-left: 0 !important;
    padding-right: 0 !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
    width: 100% !important;
    max-width: 100% !important;
    box-sizing: border-box !important;
  }

  #product-recommendations-{{ section.id }} {
    --carousel-gap: {{ section.settings.carousel_gap | default: 20 }}px;
    --carousel-padding-horizontal: {{ section.settings.padding_horizontal | default: 8 }}px;
    --cols-desktop: {{ cols_desktop }};
    --cols-tablet: {{ cols_tablet }};
    --cols-mobile: {{ cols_mobile }};
    --product-info-left-offset: 0px;
    --collection-carousel-aspect-ratio: {{ aspect_ratio }};
    --desktop-image-height: {{ desktop_image_height }};
    --quick-add-icon-color: {{ section.settings.quick_add_icon_color | default: '#ffffff' }};
    width: 100% !important;
    max-width: 100% !important;
    padding-left: 0 !important;
    padding-right: 0 !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
    box-sizing: border-box !important;
  }

  #product-recommendations-{{ section.id }} .collection-hover-carousel__quick-add-btn {
    background-color: transparent !important;
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
    color: var(--quick-add-icon-color) !important;
    transition: transform 0.2s ease, opacity 0.2s ease, color 0.2s ease !important;
  }
  #product-recommendations-{{ section.id }} .collection-hover-carousel__quick-add-btn::after {
    display: none !important;
  }
  #product-recommendations-{{ section.id }} .collection-hover-carousel__quick-add-btn:hover {
    transform: scale(1.05) !important;
    background-color: transparent !important;
    background: transparent !important;
    color: var(--quick-add-icon-color) !important;
  }
  #product-recommendations-{{ section.id }} .collection-hover-carousel__quick-add-btn.is-success {
    background-color: transparent !important;
    background: transparent !important;
    color: #10b981 !important;
    border: none !important;
    box-shadow: none !important;
  }
  #product-recommendations-{{ section.id }} .btn-spinner {
    border-color: rgba(255, 255, 255, 0.15) !important;
    border-top-color: currentColor !important;
  }
  #product-recommendations-{{ section.id }} .collection-hover-carousel__title-link {
    text-decoration: none;
    color: inherit;
  }
  #product-recommendations-{{ section.id }} .collection-hover-carousel__title-link:hover {
    text-decoration: underline;
  }
  #product-recommendations-{{ section.id }} .collection-hover-carousel__card-link-overlay {
    z-index: 2 !important;
  }

  #product-recommendations-{{ section.id }} .page-width {
    padding-left: 0 !important;
    padding-right: 0 !important;
    max-width: 100% !important;
    width: 100% !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
  }

  #product-recommendations-{{ section.id }} .collection-hover-carousel__header {
    padding-left: calc(var(--carousel-padding-horizontal, 0px) + var(--page-margin, 2rem)) !important;
    padding-right: calc(var(--carousel-padding-horizontal, 0px) + var(--page-margin, 2rem)) !important;
  }

  #product-recommendations-{{ section.id }} .collection-hover-carousel__track {
    padding-left: var(--carousel-padding-horizontal, 0px) !important;
    padding-right: var(--carousel-padding-horizontal, 0px) !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
    width: 100% !important;
  }

  @media screen and (max-width: 768px) {
    #product-recommendations-{{ section.id }} .collection-hover-carousel__track {
      padding-left: var(--carousel-padding-horizontal, 0px) !important;
      padding-right: var(--carousel-padding-horizontal, 0px) !important;
    }
    #product-recommendations-{{ section.id }} .collection-hover-carousel__header {
      padding-left: calc(var(--carousel-padding-horizontal, 0px) + 2rem) !important;
      padding-right: calc(var(--carousel-padding-horizontal, 0px) + 2rem) !important;
    }
  }

  @media screen and (max-width: 480px) {
    #product-recommendations-{{ section.id }} .collection-hover-carousel__item {
      flex: 0 0 70%; /* Shows 1.4 cards to indicate scrolling */
    }
  }

  @media screen and (min-width: 750px) {
    {% if section.settings.desktop_card_height != 'adapt' %}
      #product-recommendations-{{ section.id }} .collection-hover-carousel__image-wrapper {
        height: var(--desktop-image-height) !important;
        max-height: var(--desktop-image-height) !important;
        aspect-ratio: auto !important;
      }
      #product-recommendations-{{ section.id }} .collection-hover-carousel__img {
        object-fit: cover !important;
        height: 100% !important;
        width: 100% !important;
      }
    {% endif %}
  }

  /* Section Title and Product Typography Mappings */
  #product-recommendations-{{ section.id }} .collection-hover-carousel__title {
    {% if section.settings.collection_title_font == 'body' %}
      font-family: var(--font-paragraph--family) !important;
      font-size: var(--font-paragraph--size, 1.6rem) !important;
      font-weight: var(--font-paragraph--weight, 400) !important;
      text-transform: var(--font-paragraph--case, none) !important;
    {% elsif section.settings.collection_title_font == 'subheading' %}
      font-family: var(--font-subheading--family) !important;
      font-size: var(--font-subheading--size, 1.8rem) !important;
      font-weight: var(--font-subheading--weight, 600) !important;
      text-transform: var(--font-subheading--case, none) !important;
    {% elsif section.settings.collection_title_font == 'heading' %}
      font-family: var(--font-heading--family) !important;
      font-size: var(--font-heading--size, 2rem) !important;
      font-weight: var(--font-heading--weight, 700) !important;
      text-transform: var(--font-heading--case, none) !important;
    {% elsif section.settings.collection_title_font == 'accent' %}
      font-family: var(--font-accent--family) !important;
      font-size: var(--font-accent--size, 1.4rem) !important;
      font-weight: var(--font-accent--weight, 400) !important;
      text-transform: var(--font-accent--case, none) !important;
    {% elsif section.settings.collection_title_font == 'custom' %}
      font-size: {{ section.settings.collection_title_custom_size }}px !important;
      {% if section.settings.collection_title_custom_font == 'body' %}
        font-family: var(--font-paragraph--family) !important;
        font-weight: var(--font-paragraph--weight, 400) !important;
        text-transform: var(--font-paragraph--case, none) !important;
      {% elsif section.settings.collection_title_custom_font == 'subheading' %}
        font-family: var(--font-subheading--family) !important;
        font-weight: var(--font-subheading--weight, 600) !important;
        text-transform: var(--font-subheading--case, none) !important;
      {% elsif section.settings.collection_title_custom_font == 'heading' %}
        font-family: var(--font-heading--family) !important;
        font-weight: var(--font-heading--weight, 700) !important;
        text-transform: var(--font-heading--case, none) !important;
      {% elsif section.settings.collection_title_custom_font == 'accent' %}
        font-family: var(--font-accent--family) !important;
        font-weight: var(--font-accent--weight, 400) !important;
        text-transform: var(--font-accent--case, none) !important;
      {% endif %}
    {% endif %}
  }

  #product-recommendations-{{ section.id }} .collection-hover-carousel__product-title {
    {% if section.settings.title_font == 'body' %}
      font-family: var(--font-paragraph--family) !important;
      font-size: var(--font-paragraph--size, 1.6rem) !important;
      font-weight: var(--font-paragraph--weight, 400) !important;
      text-transform: var(--font-paragraph--case, none) !important;
    {% elsif section.settings.title_font == 'subheading' %}
      font-family: var(--font-subheading--family) !important;
      font-size: var(--font-subheading--size, 1.8rem) !important;
      font-weight: var(--font-subheading--weight, 600) !important;
      text-transform: var(--font-subheading--case, none) !important;
    {% elsif section.settings.title_font == 'heading' %}
      font-family: var(--font-heading--family) !important;
      font-size: var(--font-heading--size, 2rem) !important;
      font-weight: var(--font-heading--weight, 700) !important;
      text-transform: var(--font-heading--case, none) !important;
    {% elsif section.settings.title_font == 'accent' %}
      font-family: var(--font-accent--family) !important;
      font-size: var(--font-accent--size, 1.4rem) !important;
      font-weight: var(--font-accent--weight, 400) !important;
      text-transform: var(--font-accent--case, none) !important;
    {% elsif section.settings.title_font == 'custom' %}
      font-size: {{ section.settings.title_custom_size }}px !important;
      {% if section.settings.title_custom_font == 'body' %}
        font-family: var(--font-paragraph--family) !important;
        font-weight: var(--font-paragraph--weight, 400) !important;
        text-transform: var(--font-paragraph--case, none) !important;
      {% elsif section.settings.title_custom_font == 'subheading' %}
        font-family: var(--font-subheading--family) !important;
        font-weight: var(--font-subheading--weight, 600) !important;
        text-transform: var(--font-subheading--case, none) !important;
      {% elsif section.settings.title_custom_font == 'heading' %}
        font-family: var(--font-heading--family) !important;
        font-weight: var(--font-heading--weight, 700) !important;
        text-transform: var(--font-heading--case, none) !important;
      {% elsif section.settings.title_custom_font == 'accent' %}
        font-family: var(--font-accent--family) !important;
        font-weight: var(--font-accent--weight, 400) !important;
        text-transform: var(--font-accent--case, none) !important;
      {% endif %}
    {% endif %}
  }

  #product-recommendations-{{ section.id }} .collection-hover-carousel__product-price {
    {% if section.settings.price_font == 'body' %}
      font-family: var(--font-paragraph--family) !important;
      font-size: var(--font-paragraph--size, 1.6rem) !important;
      font-weight: var(--font-paragraph--weight, 400) !important;
      text-transform: var(--font-paragraph--case, none) !important;
    {% elsif section.settings.price_font == 'subheading' %}
      font-family: var(--font-subheading--family) !important;
      font-size: var(--font-subheading--size, 1.8rem) !important;
      font-weight: var(--font-subheading--weight, 600) !important;
      text-transform: var(--font-subheading--case, none) !important;
    {% elsif section.settings.price_font == 'heading' %}
      font-family: var(--font-heading--family) !important;
      font-size: var(--font-heading--size, 2rem) !important;
      font-weight: var(--font-heading--weight, 700) !important;
      text-transform: var(--font-heading--case, none) !important;
    {% elsif section.settings.price_font == 'accent' %}
      font-family: var(--font-accent--family) !important;
      font-size: var(--font-accent--size, 1.4rem) !important;
      font-weight: var(--font-accent--weight, 400) !important;
      text-transform: var(--font-accent--case, none) !important;
    {% elsif section.settings.price_font == 'custom' %}
      font-size: {{ section.settings.price_custom_size }}px !important;
      {% if section.settings.price_custom_font == 'body' %}
        font-family: var(--font-paragraph--family) !important;
        font-weight: var(--font-paragraph--weight, 400) !important;
        text-transform: var(--font-paragraph--case, none) !important;
      {% elsif section.settings.price_custom_font == 'subheading' %}
        font-family: var(--font-subheading--family) !important;
        font-weight: var(--font-subheading--weight, 600) !important;
        text-transform: var(--font-subheading--case, none) !important;
      {% elsif section.settings.price_custom_font == 'heading' %}
        font-family: var(--font-heading--family) !important;
        font-weight: var(--font-heading--weight, 700) !important;
        text-transform: var(--font-heading--case, none) !important;
      {% elsif section.settings.price_custom_font == 'accent' %}
        font-family: var(--font-accent--family) !important;
        font-weight: var(--font-accent--weight, 400) !important;
        text-transform: var(--font-accent--case, none) !important;
      {% endif %}
    {% endif %}
  }

  /* Quick Add Overlay and plus button styling */
  .collection-hover-carousel__quick-add {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 3;
    pointer-events: none;
  }

  .collection-hover-carousel__quick-add-btn {
    position: absolute;
    bottom: 12px;
    right: 12px;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background-color: #ffffff;
    color: #000000;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 10;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    transition: transform 0.2s ease, opacity 0.2s ease, background-color 0.2s ease, color 0.2s ease;
    opacity: 0;
    transform: scale(0.9);
    pointer-events: auto;
    padding: 0;
    overflow: hidden;
  }

  .collection-hover-carousel__quick-add-btn::after {
    content: '';
    position: absolute;
    top: 0;
    left: -150%;
    width: 50%;
    height: 100%;
    background: linear-gradient(
      90deg,
      rgba(255, 255, 255, 0) 0%,
      rgba(255, 255, 255, 0.4) 50%,
      rgba(255, 255, 255, 0) 100%
    );
    transform: skewX(-20deg);
    pointer-events: none;
    transition: none;
  }

  @media (hover: hover) {
    .collection-hover-carousel__quick-add-btn:hover::after {
      left: 150%;
      transition: left 0.65s cubic-bezier(0.16, 1, 0.3, 1);
    }
  }

  .collection-hover-carousel__quick-add-btn:hover {
    transform: scale(1.05);
    background-color: #000000;
    color: #ffffff;
  }

  .collection-hover-carousel__quick-add-btn svg {
    display: block;
    width: 14px;
    height: 14px;
  }

  /* Show plus button on card hover */
  @media (hover: hover) {
    .collection-hover-carousel__card:hover .collection-hover-carousel__quick-add-btn {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Always show plus button on touch devices */
  @media (hover: none) {
    .collection-hover-carousel__quick-add-btn {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Spinner element for loading */
  .btn-spinner {
    position: absolute;
    width: 18px;
    height: 18px;
    border: 2px solid rgba(0,0,0,0.1);
    border-top-color: currentColor;
    border-radius: 50%;
    animation: spinner-rotate 0.6s linear infinite;
  }

  .btn-spinner.hidden {
    display: none;
  }

  @keyframes spinner-rotate {
    to { transform: rotate(360deg); }
  }

  /* Sizes overlay container */
  .collection-hover-carousel__sizes-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: transparent;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    color: #000000;
    z-index: 15;
    transform: translateY(100%);
    transition: transform 0.35s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    padding: 6px 0;
    box-shadow: none;
    display: flex;
    justify-content: center;
    align-items: center;
    pointer-events: auto;
  }

  /* Open/Active state */
  .collection-hover-carousel__quick-add.is-active .collection-hover-carousel__sizes-overlay {
    transform: translateY(0);
  }

  /* Hide plus button when size options overlay is open */
  .collection-hover-carousel__quick-add.is-active .collection-hover-carousel__quick-add-btn {
    opacity: 0 !important;
    transform: scale(0.8) !important;
    pointer-events: none !important;
    visibility: hidden !important;
  }

  .collection-hover-carousel__sizes-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 6px;
    width: 100%;
  }

  .collection-hover-carousel__size-opt {
    flex: 0 0 auto;
    min-width: 42px;
    height: 32px;
    border: 1px solid #d1d5db;
    background-color: #f3f4f6;
    color: #111827;
    font-family: inherit;
    font-size: 1.2rem;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    border-radius: 4px;
    padding: 0 10px;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
  }

  .collection-hover-carousel__size-opt:hover:not(:disabled) {
    background-color: #111827;
    color: #ffffff;
    border-color: #111827;
    transform: translateY(-1px);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.08);
  }

  .collection-hover-carousel__size-opt:active:not(:disabled),
  .collection-hover-carousel__size-opt.is-active {
    background-color: #000000;
    color: #ffffff;
    border-color: #000000;
    transform: scale(0.95);
  }

  /* Strikethrough for unavailable sizes */
  .collection-hover-carousel__size-opt:disabled,
  .collection-hover-carousel__size-opt.is-unavailable {
    opacity: 0.35;
    background-color: #f9fafb;
    color: #9ca3af;
    border-color: #e5e7eb;
    cursor: not-allowed;
    position: relative;
    overflow: hidden;
    box-shadow: none;
    transform: none !important;
  }

  .collection-hover-carousel__size-opt.is-unavailable::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 0;
    width: 100%;
    height: 1.5px;
    background-color: #9ca3af;
    transform: rotate(-45deg);
  }

  /* Success pop animation */
  .collection-hover-carousel__quick-add-btn.is-success,
  .collection-hover-carousel__size-opt.is-success {
    background-color: #10b981 !important;
    color: #ffffff !important;
    border-color: #10b981 !important;
    animation: success-pop 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
  }

  @keyframes success-pop {
    0% { transform: scale(0.8); }
    50% { transform: scale(1.15); }
    100% { transform: scale(1); }
  }

  .collection-hover-carousel {
    padding: 8px 0;
    background-color: var(--collection-carousel-bg, var(--color-background, #000000));
    color: var(--collection-carousel-text, var(--color-foreground, #ffffff));
    overflow: hidden;
    font-family: var(--font-body--family), sans-serif;
  }

  .collection-hover-carousel__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    margin-bottom: 0;
  }

  .collection-hover-carousel__heading {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
    width: 100%;
  }

  .collection-hover-carousel__title {
    margin-top: 0;
    margin-bottom: 0;
    margin-left: 0;
    margin-right: 0;
    font-size: 2.4rem;
    font-weight: 500;
    letter-spacing: -0.02em;
    color: var(--color-foreground-heading, inherit);
    text-transform: uppercase;
    opacity: 0;
    transition: opacity 700ms ease, transform 700ms ease;
    will-change: opacity, transform;
    width: 100%;
  }

  .collection-hover-carousel__header.title-visible .collection-hover-carousel__title {
    opacity: 1;
    transform: none;
  }

  .collection-hover-carousel__nav {
    display: flex;
    gap: 1rem;
  }

  .collection-hover-carousel__btn {
    background: transparent;
    border: 1px solid var(--color-border, rgba(var(--color-foreground-rgb), 0.15));
    color: var(--color-foreground, #ffffff);
    width: 42px;
    height: 42px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.25s ease;
    padding: 0;
  }

  .collection-hover-carousel__btn:hover {
    background-color: var(--color-foreground, #ffffff);
    color: var(--color-background, #000000);
    border-color: var(--color-foreground, #ffffff);
  }

  .collection-hover-carousel__btn svg {
    width: 18px;
    height: 18px;
  }

  .collection-hover-carousel__track-wrapper {
    position: relative;
    width: 100%;
  }

  .collection-hover-carousel__track {
    display: flex;
    gap: var(--carousel-gap, 2rem);
    overflow-x: auto;
    scroll-behavior: smooth;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none; /* Firefox */
  }

  .collection-hover-carousel__track::-webkit-scrollbar {
    display: none; /* Safari and Chrome */
  }

  .collection-hover-carousel__item {
    flex: 0 0 calc((100% - (var(--cols-desktop) - 1) * var(--carousel-gap, 20px)) / var(--cols-desktop));
    scroll-snap-align: start;
  }

  .collection-hover-carousel__card {
    display: flex;
    flex-direction: column;
    position: relative;
    text-decoration: none;
    color: inherit;
  }

  .collection-hover-carousel__card-link-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .collection-hover-carousel__card product-swatches {
    position: relative;
    z-index: 2;
  }

  .collection-hover-carousel__image-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: var(--collection-carousel-aspect-ratio, 3 / 4);
    overflow: hidden;
    background-color: transparent;
    margin-bottom: 1.2rem;
  }

  .collection-hover-carousel__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: opacity 0.5s cubic-bezier(0.4, 0, 0.2, 1), transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    position: absolute;
    top: 0;
    left: 0;
  }

  .collection-hover-carousel__img--hover {
    opacity: 0;
    z-index: 1;
  }

  .collection-hover-carousel__card:hover .has-hover-image .collection-hover-carousel__img--hover {
    opacity: 1;
    z-index: 2;
  }

  .collection-hover-carousel__card:hover .has-hover-image .collection-hover-carousel__img--primary {
    opacity: 0;
    transform: scale(1.03);
  }

  .collection-hover-carousel__card:hover .collection-hover-carousel__image-wrapper:not(.has-hover-image) .collection-hover-carousel__img--primary {
    transform: scale(1.03);
  }

  .collection-hover-carousel__placeholder-svg {
    width: 100%;
    height: 100%;
    background-color: transparent;
    fill: var(--color-foreground-muted, #222222);
  }

  .collection-hover-carousel__info {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .collection-hover-carousel__product-title {
    margin: 0;
    font-size: 1.4rem;
    font-weight: 400;
    color: var(--color-foreground-heading, var(--color-foreground, inherit));
    letter-spacing: normal;
    text-transform: none;
  }

  .collection-hover-carousel__product-price {
    margin: 0;
    font-family: var(--font-body--family), sans-serif;
    font-size: var(--font-paragraph--size);
    line-height: var(--font-paragraph--line-height);
    font-weight: var(--font-weight-regular);
    color: var(--color-foreground-muted, rgba(var(--color-foreground-rgb), 0.6));
  }

  .collection-hover-carousel__info--justify {
    flex-direction: row !important;
    justify-content: space-between !important;
    align-items: flex-start !important;
    gap: 1rem !important;
    margin-bottom: 0.4rem !important;
    width: 100%;
  }

  .collection-hover-carousel__info--justify .collection-hover-carousel__product-title {
    flex: 1 !important;
    min-width: 0 !important;
    text-align: left !important;
  }

  .collection-hover-carousel__info--justify .collection-hover-carousel__product-price {
    flex-shrink: 0 !important;
    text-align: right !important;
    white-space: nowrap !important;
  }


  /* Responsiveness */
  @media screen and (max-width: 1024px) {
    .collection-hover-carousel__item {
      flex: 0 0 calc((100% - (var(--cols-tablet) - 1) * var(--carousel-gap, 20px)) / var(--cols-tablet));
    }
  }

  @media screen and (max-width: 768px) {
    .collection-hover-carousel__item {
      flex: 0 0 calc((100% - (var(--cols-mobile) - 1) * var(--carousel-gap, 20px)) / var(--cols-mobile));
    }

    .collection-hover-carousel .page-width {
      padding-left: 0;
      padding-right: 0;
    }

    .collection-hover-carousel__header {
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding-left: 2rem;
      padding-right: 2rem;
      margin-bottom: 0;
    }

    .collection-hover-carousel__heading {
      justify-content: center;
    }

    .collection-hover-carousel__nav {
      display: none;
    }

    .collection-hover-carousel__track {
      padding-left: var(--carousel-padding-horizontal, 2rem);
      padding-right: var(--carousel-padding-horizontal, 2rem);
    }
  }

  @media screen and (max-width: 480px) {
    .collection-hover-carousel__title {
      font-size: 1.8rem;
    }

    .collection-hover-carousel__btn {
      width: 36px;
      height: 36px;
    }
  }

  /* Skeleton Loading State Animation */
  .skeleton-glow {
    animation: skeleton-glow-pulse 1.5s ease-in-out infinite;
  }
  @keyframes skeleton-glow-pulse {
    0% { opacity: 0.3; }
    50% { opacity: 0.6; }
    100% { opacity: 0.3; }
  }
{% endstyle %}

<product-recommendations
  id="product-recommendations-{{ section.id }}"
  class="product-recommendations"
  data-url="{{ routes.product_recommendations_url }}?limit={{ section.settings.max_products }}"
  data-section-id="{{ section.id }}"
  data-product-id="{{ section.settings.product.id | default: product.id }}"
  data-intent="{{ section.settings.recommendation_type }}"
  data-testid="product-recommendations-section"
  data-recommendations-performed="{% if has_custom_products %}true{% else %}{{ recommendations.performed }}{% endif %}"
  {{ section.shopify_attributes }}
>
  <div class="section-background color-{{ section.settings.color_scheme }}"></div>

  {%- if has_custom_products or recommendations.performed or section.settings.product == blank -%}
    {% liquid
      if relic_collection != blank
        assign products = relic_collection.products | reject: 'id', product.id
      elsif section.settings.collection != blank
        assign products = section.settings.collection.products | reject: 'id', product.id
      elsif section.settings.product == blank
        assign products = null
        # Onboarding mode: Show placeholder products
        for i in (1..section.settings.max_products)
          assign products = products | append: ', '
          assign products = products | split: ','
        endfor
      elsif recommendations.performed and recommendations.products_count == 0
        # No recommendations found, pull from catalog
        if section.settings.recommendation_type == 'related'
          assign products = collections.all.products | reject: 'id', section.settings.product.id
        elsif section.settings.recommendation_type == 'complementary'
          assign products = null
        endif
      else
        assign products = recommendations.products
      endif

      assign section_title = section.settings.title
      if section_title == blank
        for block in section.blocks
          if block.type == 'text' and block.settings.text != blank
            assign section_title = block.settings.text
            break
          endif
        endfor
      endif
      if section_title == blank
        assign section_title = 'Recommended Products'
      endif
    %}

    <div class="collection-hover-carousel {% if section.settings.color_scheme != blank %}color-{{ section.settings.color_scheme }}{% endif %}">
      <div class="page-width">
        <!-- Header with Title and Navigation Controls -->
        <div class="collection-hover-carousel__header collection-hover-carousel__header--fade-up" style="--title-animation-duration: 700ms;">
          <div class="collection-hover-carousel__heading">
            <h2 class="collection-hover-carousel__title" style="margin-top: {{ section.settings.title_margin_top }}px; margin-bottom: {{ section.settings.title_margin_bottom }}px; text-align: {{ section.settings.heading_alignment }};">{{ section_title }}</h2>
          </div>
          {% if false %}
            <div class="collection-hover-carousel__nav">
              <button 
                class="collection-hover-carousel__btn collection-hover-carousel__btn--prev"
                aria-label="Previous slide"
              >
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <line x1="19" y1="12" x2="5" y2="12"></line>
                  <polyline points="12 19 5 12 12 5"></polyline>
                </svg>
              </button>
              <button 
                class="collection-hover-carousel__btn collection-hover-carousel__btn--next"
                aria-label="Next slide"
              >
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <line x1="5" y1="12" x2="19" y2="12"></line>
                  <polyline points="12 5 19 12 12 19"></polyline>
                </svg>
              </button>
            </div>
          {% endif %}
        </div>

        <!-- Carousel Track Wrapper -->
        <div class="collection-hover-carousel__track-wrapper">
          <div class="collection-hover-carousel__track{% unless section.settings.enable_carousel %} collection-hover-carousel__track--grid{% endunless %}">
            {% if products != blank and products.size > 0 %}
              {% for product in products limit: section.settings.max_products %}
                {% liquid
                  assign title = product.title | default: 'Sample Product'
                  assign price_text = product.price | money_without_trailing_zeros
                  assign primary_image = product.featured_image
                  assign hover_image = nil
                  if section.settings.show_secondary_image and product.images.size > 1
                    assign hover_image = product.images[1]
                  endif
                  assign card_url = product.url
                %}
                <div class="collection-hover-carousel__item">
                  <div class="collection-hover-carousel__card">
                    <div class="collection-hover-carousel__image-wrapper{% if hover_image != blank and hover_image != primary_image %} has-hover-image{% endif %}">
                      {% if primary_image != blank %}
                        {{ primary_image 
                          | image_url: width: 1200 
                          | image_tag: 
                            loading: 'lazy', 
                            sizes: '(min-width: 750px) 25vw, 50vw', 
                            widths: '200, 300, 400, 500, 600, 800, 1000, 1200', 
                            class: 'collection-hover-carousel__img collection-hover-carousel__img--primary',
                            alt: title
                        }}
                        {% if hover_image != blank and hover_image != primary_image %}
                          {{ hover_image 
                            | image_url: width: 1200 
                            | image_tag: 
                              loading: 'lazy', 
                              sizes: '(min-width: 750px) 25vw, 50vw', 
                              widths: '200, 300, 400, 500, 600, 800, 1000, 1200', 
                              class: 'collection-hover-carousel__img collection-hover-carousel__img--hover',
                              alt: ''
                          }}
                        {% endif %}
                      {% else %}
                        {{ 'product-' | append: forloop.index | placeholder_svg_tag: 'collection-hover-carousel__placeholder-svg' }}
                      {% endif %}

                      <a href="{{ card_url }}" class="collection-hover-carousel__card-link-overlay" aria-label="{{ title | escape }}"></a>

                      {% comment %} --- Custom Quick Add Sizes Overlay --- {% endcomment %}
                      {%- assign size_option = nil -%}
                      {%- assign size_option_index = -1 -%}
                      {%- for option in product.options_with_values -%}
                        {%- assign name_downcase = option.name | downcase -%}
                        {%- if name_downcase == 'size' or name_downcase == 'sizes' or name_downcase == 'taille' or name_downcase == 'größe' or name_downcase == 'talla' -%}
                          {%- assign size_option = option -%}
                          {%- assign size_option_index = forloop.index0 -%}
                          {%- break -%}
                        {%- endif -%}
                      {%- endfor -%}

                      <div class="collection-hover-carousel__quick-add" data-product-id="{{ product.id }}">
                        <button class="collection-hover-carousel__quick-add-btn" aria-label="Add to cart / Select size" type="button">
                          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="12" y1="5" x2="12" y2="19"></line>
                            <line x1="5" y1="12" x2="19" y2="12"></line>
                          </svg>
                          <span class="btn-spinner hidden"></span>
                        </button>
                        
                        {%- if size_option != blank -%}
                          <div class="collection-hover-carousel__sizes-overlay">
                            <div class="collection-hover-carousel__sizes-list">
                              {%- for value in size_option.values -%}
                                <button 
                                  class="collection-hover-carousel__size-opt" 
                                  data-size-value="{{ value | escape }}"
                                  data-size-index="{{ size_option_index }}"
                                  type="button"
                                >
                                  {{ value }}
                                </button>
                              {%- endfor -%}
                            </div>
                          </div>
                          
                          <script type="application/json" class="product-variants-json">
                            {{ product.variants | json }}
                          </script>
                        {%- else -%}
                          <script type="application/json" class="product-variants-json">
                            {{ product.variants | json }}
                          </script>
                        {%- endif -%}
                      </div>
                    </div>
                    <div class="collection-hover-carousel__info{% if section.settings.justify_title_price %} collection-hover-carousel__info--justify{% endif %}">
                      <h4 class="collection-hover-carousel__product-title"><a href="{{ card_url }}" class="collection-hover-carousel__title-link">{{ title }}</a></h4>
                      <p class="collection-hover-carousel__product-price">{{ price_text }}</p>
                    </div>
                    
                    {% liquid
                      assign product_has_swatches = false
                      for product_option in product.options_with_values
                        assign swatch_count = product_option.values | map: 'swatch' | compact | size
                        assign option_name_downcase = product_option.name | downcase
                        if swatch_count > 0 or option_name_downcase == 'color' or option_name_downcase == 'colour' or option_name_downcase == 'farbe' or option_name_downcase == 'couleur'
                          assign product_has_swatches = true
                        endif
                      endfor
                    %}
                    {% if section.settings.show_swatches and product_has_swatches %}
                      <product-swatches
                        data-product-id="{{ product.id }}"
                        data-product-url="{{ product.url }}"
                        style="
                          --overflow-list-alignment: {{ section.settings.swatches_alignment }};
                          --overflow-list-alignment-mobile: {{ section.settings.swatches_alignment_mobile }};
                          --product-swatches-alignment: {% if section.settings.swatches_alignment == 'flex-start' %}start{% elsif section.settings.swatches_alignment == 'flex-end' %}end{% else %}center{% endif %};
                          --product-swatches-alignment-mobile: {% if section.settings.swatches_alignment_mobile == 'flex-start' %}start{% elsif section.settings.swatches_alignment_mobile == 'flex-end' %}end{% else %}center{% endif %};
                          --product-swatches-padding-block-start: {{ section.settings.swatches_padding_top }}px;
                          --product-swatches-padding-block-end: {{ section.settings.swatches_padding_bottom }}px;
                          --product-swatches-padding-inline-start: {{ section.settings.swatches_padding_left }}px;
                          --product-swatches-padding-inline-end: {{ section.settings.swatches_padding_right }}px;
                          --variant-picker-swatch-radius: {{ section.settings.swatches_border_radius }}px;
                          --style-border-swatch-width: {{ section.settings.swatches_border_width }}px;
                          --style-border-swatch-style: {{ section.settings.swatches_border_style }};
                          --color-border: {{ section.settings.swatches_border_color }};
                        "
                      >
                        {% render 'variant-swatches', product_resource: product %}
                      </product-swatches>
                    {% endif %}
                  </div>
                </div>
              {% endfor %}
            {% else %}
              {% comment %} Placeholder items if no products are found {% endcomment %}
              {% for i in (1..section.settings.max_products) %}
                {% assign placeholder_index = i | modulo: 4 | plus: 1 %}
                <div class="collection-hover-carousel__item">
                  <div class="collection-hover-carousel__card">
                    <div class="collection-hover-carousel__image-wrapper">
                      {{ 'product-' | append: placeholder_index | placeholder_svg_tag: 'collection-hover-carousel__placeholder-svg' }}
                      <a href="#" class="collection-hover-carousel__card-link-overlay" aria-label="Sample Product {{ i }}"></a>
                    </div>
                    <div class="collection-hover-carousel__info{% if section.settings.justify_title_price %} collection-hover-carousel__info--justify{% endif %}">
                      <h4 class="collection-hover-carousel__product-title"><a href="#" class="collection-hover-carousel__title-link">Sample Product {{ i }}</a></h4>
                      <p class="collection-hover-carousel__product-price">Rs. 1,200 INR</p>
                    </div>
                  </div>
                </div>
              {% endfor %}
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  {%- else -%}
    {% comment %} Skeleton glow loaders before perform API callback {% endcomment %}
    <div class="collection-hover-carousel">
      <div class="page-width">
        <div class="collection-hover-carousel__track{% unless section.settings.enable_carousel %} collection-hover-carousel__track--grid{% endunless %}">
          {% for i in (1..section.settings.columns) %}
            <div class="collection-hover-carousel__item">
              <div class="collection-hover-carousel__card">
                <div class="collection-hover-carousel__image-wrapper skeleton-glow" style="aspect-ratio: {{ aspect_ratio }}; background-color: rgba(var(--color-foreground-rgb), 0.08); height: {{ desktop_image_height }}"></div>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  {%- endif -%}
</product-recommendations>

{% schema %}
{
  "name": "t:names.product_recommendations",
  "tag": "section",
  "class": "section-product-recommendations-wrapper",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "blocks": [
    {
      "type": "@theme"
    },
    {
      "type": "@app"
    },
    {
      "type": "text"
    },
    {
      "type": "icon"
    },
    {
      "type": "image"
    },
    {
      "type": "button"
    },
    {
      "type": "video"
    },
    {
      "type": "group"
    },
    {
      "type": "spacer"
    },
    {
      "type": "_divider"
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "Quick Add Icon"
    },
    {
      "type": "color",
      "id": "quick_add_icon_color",
      "label": "Icon Color",
      "default": "#ffffff"
    },
    {
      "type": "product",
      "id": "product",
      "label": "t:settings.product"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Custom Collection Override",
      "info": "Display products from this collection instead of dynamic recommendations."
    },
    {
      "type": "select",
      "id": "recommendation_type",
      "label": "t:settings.type",
      "options": [
        {
          "value": "related",
          "label": "t:options.related"
        },
        {
          "value": "complementary",
          "label": "t:options.complementary"
        }
      ],
      "default": "related"
    },
    {
      "type": "paragraph",
      "content": "t:content.complementary_products"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Recommended Products"
    },
    {
      "type": "range",
      "id": "max_products",
      "label": "t:settings.product_count",
      "min": 3,
      "max": 24,
      "step": 1,
      "default": 8
    },
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "checkbox",
      "id": "enable_carousel",
      "label": "Enable Carousel (horizontal scroll)",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "justify_title_price",
      "label": "Justify Title & Price side-by-side",
      "default": true
    },
    {
      "type": "range",
      "id": "columns",
      "label": "t:settings.columns",
      "min": 1,
      "max": 8,
      "step": 1,
      "default": 4
    },
    {
      "type": "select",
      "id": "mobile_columns",
      "label": "t:settings.mobile_columns",
      "options": [
        {
          "value": "1",
          "label": "t:options.one_number"
        },
        {
          "value": "2",
          "label": "t:options.two_number"
        }
      ],
      "default": "2"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "show_secondary_image",
      "label": "Show second image on hover",
      "default": true
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "Image ratio",
      "options": [
        {
          "value": "adapt",
          "label": "Adapt to image"
        },
        {
          "value": "square",
          "label": "Square (1:1)"
        },
        {
          "value": "portrait",
          "label": "Portrait (3:4)"
        },
        {
          "value": "portrait-alt",
          "label": "Portrait (4:5)"
        },
        {
          "value": "landscape",
          "label": "Landscape (16:9)"
        }
      ],
      "default": "portrait"
    },
    {
      "type": "select",
      "id": "desktop_card_height",
      "label": "Desktop Card Image Height",
      "options": [
        {
          "value": "adapt",
          "label": "Adapt to image ratio"
        },
        {
          "value": "small",
          "label": "Small (280px)"
        },
        {
          "value": "medium",
          "label": "Medium (380px)"
        },
        {
          "value": "large",
          "label": "Large (480px)"
        },
        {
          "value": "custom",
          "label": "Custom height"
        }
      ],
      "default": "adapt"
    },
    {
      "type": "range",
      "id": "desktop_card_custom_height",
      "min": 200,
      "max": 600,
      "step": 10,
      "unit": "px",
      "label": "Custom Desktop Image Height",
      "default": 380
    },
    {
      "type": "range",
      "id": "padding_horizontal",
      "label": "Horizontal Padding (Side Padding)",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "carousel_gap",
      "label": "Gap between product images",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "default": 20
    },
    {
      "type": "checkbox",
      "id": "show_swatches",
      "label": "Show variant swatches",
      "default": false
    },
    {
      "type": "select",
      "id": "swatches_alignment",
      "label": "Swatches Alignment",
      "options": [
        {
          "value": "flex-start",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "flex-end",
          "label": "Right"
        }
      ],
      "default": "flex-start"
    },
    {
      "type": "select",
      "id": "swatches_alignment_mobile",
      "label": "Swatches Alignment Mobile",
      "options": [
        {
          "value": "flex-start",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "flex-end",
          "label": "Right"
        }
      ],
      "default": "flex-start"
    },
    {
      "type": "range",
      "id": "swatches_padding_top",
      "label": "Padding Top",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 4
    },
    {
      "type": "range",
      "id": "swatches_padding_bottom",
      "label": "Padding Bottom",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "swatches_padding_left",
      "label": "Padding Left",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "swatches_padding_right",
      "label": "Padding Right",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "swatches_border_radius",
      "label": "Border radius",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 4
    },
    {
      "type": "range",
      "id": "swatches_border_width",
      "label": "Border thickness",
      "min": 0,
      "max": 10,
      "step": 1,
      "unit": "px",
      "default": 1
    },
    {
      "type": "select",
      "id": "swatches_border_style",
      "label": "Border style",
      "options": [
        {
          "value": "solid",
          "label": "Solid"
        },
        {
          "value": "dashed",
          "label": "Dashed"
        },
        {
          "value": "dotted",
          "label": "Dotted"
        },
        {
          "value": "none",
          "label": "None"
        }
      ],
      "default": "solid"
    },
    {
      "type": "color",
      "id": "swatches_border_color",
      "label": "Border color",
      "default": "#dddddd"
    },
    {
      "type": "header",
      "content": "Typography Settings"
    },
    {
      "type": "select",
      "id": "collection_title_font",
      "label": "Section title typography preset",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "body",
          "label": "Body"
        },
        {
          "value": "subheading",
          "label": "Subheading"
        },
        {
          "value": "heading",
          "label": "Heading"
        },
        {
          "value": "accent",
          "label": "Accent"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "default"
    },
    {
      "type": "paragraph",
      "content": "Edit presets in theme settings",
      "visible_if": "{{ section.settings.collection_title_font == 'custom' }}"
    },
    {
      "type": "select",
      "id": "collection_title_custom_font",
      "label": "Font",
      "options": [
        {
          "value": "heading",
          "label": "Heading"
        },
        {
          "value": "subheading",
          "label": "Subheading"
        },
        {
          "value": "accent",
          "label": "Accent"
        },
        {
          "value": "body",
          "label": "Body"
        }
      ],
      "default": "heading",
      "visible_if": "{{ section.settings.collection_title_font == 'custom' }}"
    },
    {
      "type": "range",
      "id": "collection_title_custom_size",
      "min": 10,
      "max": 80,
      "step": 1,
      "unit": "px",
      "label": "Section title custom font size",
      "default": 24,
      "visible_if": "{{ section.settings.collection_title_font == 'custom' }}"
    },
    {
      "type": "select",
      "id": "title_font",
      "label": "Product title typography preset",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "body",
          "label": "Body"
        },
        {
          "value": "subheading",
          "label": "Subheading"
        },
        {
          "value": "heading",
          "label": "Heading"
        },
        {
          "value": "accent",
          "label": "Accent"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "default"
    },
    {
      "type": "paragraph",
      "content": "Edit presets in theme settings",
      "visible_if": "{{ section.settings.title_font == 'custom' }}"
    },
    {
      "type": "select",
      "id": "title_custom_font",
      "label": "Font",
      "options": [
        {
          "value": "heading",
          "label": "Heading"
        },
        {
          "value": "subheading",
          "label": "Subheading"
        },
        {
          "value": "accent",
          "label": "Accent"
        },
        {
          "value": "body",
          "label": "Body"
        }
      ],
      "default": "body",
      "visible_if": "{{ section.settings.title_font == 'custom' }}"
    },
    {
      "type": "range",
      "id": "title_custom_size",
      "min": 10,
      "max": 80,
      "step": 1,
      "unit": "px",
      "label": "Product title custom font size",
      "default": 14,
      "visible_if": "{{ section.settings.title_font == 'custom' }}"
    },
    {
      "type": "select",
      "id": "price_font",
      "label": "Product price typography preset",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "body",
          "label": "Body"
        },
        {
          "value": "subheading",
          "label": "Subheading"
        },
        {
          "value": "heading",
          "label": "Heading"
        },
        {
          "value": "accent",
          "label": "Accent"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "default"
    },
    {
      "type": "paragraph",
      "content": "Edit presets in theme settings",
      "visible_if": "{{ section.settings.price_font == 'custom' }}"
    },
    {
      "type": "select",
      "id": "price_custom_font",
      "label": "Font",
      "options": [
        {
          "value": "heading",
          "label": "Heading"
        },
        {
          "value": "subheading",
          "label": "Subheading"
        },
        {
          "value": "accent",
          "label": "Accent"
        },
        {
          "value": "body",
          "label": "Body"
        }
      ],
      "default": "body",
      "visible_if": "{{ section.settings.price_font == 'custom' }}"
    },
    {
      "type": "range",
      "id": "price_custom_size",
      "min": 10,
      "max": 80,
      "step": 1,
      "unit": "px",
      "label": "Product price custom font size",
      "default": 14,
      "visible_if": "{{ section.settings.price_font == 'custom' }}"
    },
    {
      "type": "header",
      "content": "Title Spacing"
    },
    {
      "type": "range",
      "id": "title_margin_top",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "Title margin top",
      "default": 0
    },
    {
      "type": "range",
      "id": "title_margin_bottom",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "Title margin bottom",
      "default": 16
    },
    {
      "type": "header",
      "content": "Title Alignment"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 48
    }
  ],
  "presets": [
    {
      "name": "t:names.product_recommendations",
      "category": "t:categories.products",
      "settings": {
        "product": "{{ closest.product }}",
        "recommendation_type": "related",
        "max_products": 8,
        "columns": 4,
        "mobile_columns": "2",
        "color_scheme": "scheme-1",
        "image_ratio": "portrait",
        "desktop_card_height": "adapt",
        "padding_horizontal": 8,
        "carousel_gap": 20,
        "padding-block-start": 48,
        "padding-block-end": 48
      }
    }
  ]
}
{% endschema %}
